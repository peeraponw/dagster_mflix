[project]
name = "dagster-mflix"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dagster-embedded-elt>=0.25.6",
    "dagster>=1.9.6",
]
